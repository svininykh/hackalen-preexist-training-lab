# FAQ-бот на 5 вопросов

Терминальное приложение загружает пять пар вопрос–ответ из `faq.txt`, ищет наиболее подходящий вопрос по совпадающим ключевым словам и выводит связанный ответ. Если совпадений нет, программа печатает `не знаю`.

Стек: Kotlin/JVM и Gradle. Для запуска требуется JDK 21–25 и доступ Gradle к Maven Central при первой сборке.

```bash
./gradlew build
./gradlew run
```

## Ввод вопросов

После приглашения `>` введите вопрос в обычном интерактивном терминале. Проект поддерживает JDK 21–25; Gradle передаёт стандартный ввод запущенному приложению.

Если среда запуска сразу закрывает ввод, соберите дистрибутив и запустите её напрямую:

```bash
./gradlew installDist
build/install/faq-bot-5-questions/bin/faq-bot-5-questions
```

Пример работы:

```text
> Когда начинается обучение?
Обучение начинается в 10:00 по местному времени.
```

Тесты запускаются командой:

```bash
./gradlew test
```

Итог: минимальный CLI-бот без Ktor, RAG, LLM и внешних сервисов.

## Использование AI

AI использовался как инструмент разработки.

Подробнее: [AI-USAGE.md](AI-USAGE.md)
